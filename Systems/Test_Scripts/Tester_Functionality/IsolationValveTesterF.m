%%tester functionality%%
if (time<60)
    iv_err = NoError;
else
    iv_err = Error;
end

%   if (time < 60)
%         iv_err  = Error; 
%     elseif (time <120)
%          iv_err  = NoError;
%     elseif (time <180)
%         iv_err = NoError;
%     elseif (time <240)
%         iv_err= Error;
%   end
    
  