%%tester functionality%%
  if (time < 60)
        iv_err  = Error; 
    elseif (time <120)
         iv_err  = NoError;
    elseif (time <180)
        iv_err = NoError;
    elseif (time <240)
        iv_err= Error;
    end

%%LGC functionality%%
 if(time <60) 
        iv_open = On;        
    elseif (time <120)
        iv_open = On;
    elseif (time <180)
        iv_open = Off;
    elseif (time <240)
            iv_open = Off;
    end